# URP 设置

本项目的 `Packages/manifest.json` 已包含 URP 包。

如果 Unity 没有自动生成 URP Pipeline Asset，可以在 Unity 中选择：

`Assets -> Create -> Rendering -> URP Asset (with Universal Renderer)`

然后到：

`Project Settings -> Graphics`

把生成的 URP Pipeline Asset 拖入 `Scriptable Render Pipeline Settings`。
